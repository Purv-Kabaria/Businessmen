# Sentiment & Summarization Microservice (CPU / low VRAM)
# ONNX sentiment + FlashRank rerank + Ollama (Gemma 3) for summary

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0

# ONNX sentiment (INT8)
onnxruntime>=1.18.0
numpy>=1.24.0

# Tokenizer for RoBERTa (no torch model load at inference)
transformers>=4.30.0
tokenizers>=0.19.0

# Rerank (no torch)
flashrank>=0.2.0

# Summary via Ollama (Gemma 3 in Docker)
requests>=2.31.0
